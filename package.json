{
  "name": "cracks",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "generate-types": "wrangler types",
    "build": "esbuild src/server.ts --bundle --format=esm --outdir=dist --platform=node",
    "start": "node dist/server.js",
    "start:prod": "bun run build && bun run start"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^5.1"
  },
  "devDependencies": {
    "@types/express": "^5.0",
    "@types/node": "^24.10",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.50.0"
  }
}
